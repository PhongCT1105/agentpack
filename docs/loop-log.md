# Loop log

- 2026-08-30T09:32Z P1.1: go module + cobra skeleton with version command; CI workflow parked in docs/ci/ci.yml (token lacks workflow scope — human must git mv to .github/workflows/)
- 2026-08-30T09:40Z P1.2: internal/model — ToolID/Kind/Scope enums, Component interface, Inventory, Warning + table tests
- 2026-08-30T09:48Z P1.3: claudecode adapter Detect() — binary-on-PATH + ~/.claude|~/.claude.json probes, version parsing, fixture homes
- 2026-08-30T10:02Z P1.4: claudecode Scan skills global+project — ScanScope/Skill types in model, frontmatter description, symlink-following, warnings for broken dirs
- 2026-08-30T10:15Z P1.5: claudecode Scan MCP servers — Transport/MCPServer in model, surgical mixed-file read of ~/.claude.json + .mcp.json, transport inference, warnings for local-scope/unknown/misshapen entries
- 2026-08-30T10:30Z P1.6: claudecode Scan agents/commands/rules/settings — Agent/Command/Rule/Setting in model; personal files (CLAUDE.local.md, settings.local.json) scanned as components by design, save filters later; subdirs warn
- 2026-08-30T10:45Z P1.7: codex adapter Detect() + Scan MCP from config.toml — BurntSushi/toml surgical read, global-only per matrix; noted bearer_token_env_var/http_headers for P3.5 in matrix doc
- 2026-08-30T10:58Z P1.8: codex Scan AGENTS.md global+project + prompts; extracted shared internal/adapters/mdscan (frontmatter + flat *.md dir scan) used by both adapters; nested AGENTS.md intentionally not walked
- 2026-08-30T11:20Z P1.9: agentpack scan command + internal/engine ScanAll — table grouped tool/kind/scope, --json; env/header/args/URL credential material masked in all output (pre-P2.2 hygiene); smoke-tested on real machine
- 2026-08-30T11:40Z P1.10: scan warnings — dead stdio servers (unresolvable/missing command; relative paths exempt), .bak/.old/.orig/~ debris silently ignored, unknown MCP entry keys surfaced in both adapters
- 2026-08-30T11:55Z P1.11: README — real scan usage + sample output generated from fixture trees, supported-tools table gains scan status, quick-start annotated (save/restore in progress). Phase 1 complete.
- 2026-08-30T07:18Z P2.1: internal/packio manifest types matching spec v0.1 — strict DecodeManifest (unknown fields/version/kind/multi-doc rejected), deterministic EncodeManifest, round-trip + credential-value-rejection tests; spec gains non-secret headers: field
- 2026-08-30T07:36Z P2.2: internal/secrets redactor — Classify(key,value) → Plain/Uncertain/Secret with rule ids; format regexes, segment-based key matching, charset-normalized entropy; URL and path values cap at Uncertain (SUPABASE_URL problem); .gitleaks.toml allowlists FAKE-seeded fixtures
- 2026-08-30T07:50Z P2.3: whole-pack scanner ScanPack — format/assignment/entropy channels with boundary checks and Bearer-token handling, masked excerpts (≤6-char scaled prefix), leaky+clean adversarial fixtures; release-blocking = TestReleaseBlocking_* naming + explicit CI gate step (build tags would hide tests)
- 2026-08-30T08:02Z P2.4: packio Convert (inventories → manifest + bundles + redactions + warnings; env/header→credentials split with Bearer format preservation, settings deep-redaction, slug+collision naming) and WritePack (copy bundles, ScanPack gate removes leaky packs)
- 2026-08-30T08:10Z P2.5: agentpack save --all — scan→filter personal (rule/setting .local. only)→Convert→WritePack; refuses partial packs on scan errors; smoke-tested on real machine: layer-3 gate correctly blocked save (installed third-party skill trees carry thousands of token-shaped strings — bundling-vs-referencing UX matters for P4)
- 2026-08-30T08:18Z P2.6: agentpack validate + packio.ValidatePack — schema/name/source/bundled-path/MCP-shape checks with 24-violation bad fixture, always-on secret scan, symlink ban (unscannable content can't ship), platform-independent slash-only paths; nonzero exit for CI
- 2026-08-30T08:25Z P2.7: save --review-uncertain — per-value stderr prompt (only uncertain values displayed, never confirmed secrets), empty/EOF/error answers all redact, invalid answers re-prompt; Convert callback gains componentRef; no-flag path unchanged redact-by-default with pointer to flag
- 2026-08-30T08:31Z P2.8: docs/guides/authoring.md (export + hand-authoring paths, component reference, validate rule list — examples verified to pass validate); README updated: save/validate work today, quick-start uses --all. Phase 2 complete.
- 2026-08-30T09:55Z P3.1: pack reader + read-only `restore` preview (contents, credentials, external services, project-scope notice). Recovered from the WIP left uncommitted when the night-shift workers hit a model usage limit mid-task; verified green and reviewed before landing.
- 2026-08-30T09:55Z P2.9 filed from dogfooding: `save --all` on a real machine is blocked by 8775 scanner findings, nearly all false positives from bundled skill source/docs. Scanner fails safe but is unusable on real setups.
- 2026-08-30T10:25Z P2.9 (part 1): bundling hygiene + finding classes. Root cause of the 8775-finding block was not scanner tuning but bundling policy — `save` was copying 1.1 GB of one skill including 726 MB of node_modules, a nested .git, and its test suite. Added BundleExclusion (credential carriers, vendored deps, build output, caches, test/CI dirs), lockfile entropy suppression, and split findings into blocking vs reviewable (reviewable reported, not fatal; --strict promotes). Real-machine save: 8775 → 78 findings.
- 2026-08-30T10:32Z P2.10/P2.11/P2.12: `save --exclude` escape hatch with an error naming the offending component; bundling now follows symlinks (real installs symlink SKILL.md into place — skipping them silently produced empty bundles the manifest claimed were populated, which restore caught); canonical doc placeholders (AWS EXAMPLE keys, `user:pass@` URLs) no longer trip the unwaivable format channel. Real machine now completes the full loop: save 130 components → 5.7 MB pack → restore preview with 0 issues.
- 2026-08-30T10:52Z P3.3: credential resolver (env → keychain → prompt), keychain behind an interface defaulting to Unavailable so a zero Resolver cannot touch the real store. Secrets live behind an unexported pointer with String/GoString/Format/MarshalJSON/MarshalText/MarshalYAML all refusing — Format (not just Stringer) is what closes %#v and %x. Release-blocking test checks 30 output paths; independently mutation-verified by the orchestrator.
- 2026-08-30T11:00Z P3.7: gemini-cli adapter Detect+Scan — MCP (both settings.json layouts, httpUrl/url transport split), GEMINI.md rules, portable settings subset, extensions inventoried as warnings rather than flattened into MCP components. Never-port test proves oauth_creds.json/.env/google_accounts.json are never opened or named. Config matrix corrected from what the adapter work confirmed; filed P3.11 (mdscan hoist) and P3.12 (extension modeling).
- 2026-08-30T11:10Z P3.2: apply executor — Plan/Op types, preview rendering, backup-before-write, dry run, rollback, structured merge for json/toml/yaml. Caught a real data-loss bug in review: rollback removed recorded "created" directories unconditionally, but a path is recorded before MkdirAll runs, so a plan whose dir op hit a regular file would have deleted the user's file. Guard verified non-vacuous by mutation. Backup dir layout changed off literal RFC3339 (illegal colons on Windows).
- 2026-08-30T11:20Z P3.6: cursor adapter Detect+Scan — MCP both scopes, .mdc rules with frontmatter, legacy .cursorrules, commands. Notable: Cursor writes `globs:` unquoted, which strict YAML rejects as an alias, so a naive parser calls nearly every auto-attached rule malformed. Deliberately did NOT scan AGENTS.md (Codex already models it) — cross-tool rule dedup filed as P3.14, plus P3.15 (cursor skills) and P3.16 (OAuth auth block).
