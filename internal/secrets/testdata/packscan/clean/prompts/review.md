# Review

Check the keybindings: ctrl+k and the API_URL: https://api.example.com values.
Ask for a token via the credentials flow; placeholders such as TOKEN=<your-token> are fine.
Document headers as Authorization: Bearer YOUR_TOKEN_HERE or Authorization: Bearer ${API_TOKEN}.
